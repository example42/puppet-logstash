input {
  file {
    type => "syslog"
    path => [ "/var/log/*.log", "/var/log/messages", "/var/log/syslog" ]
  }
}

output {
  stdout {
    debug => true
  }
  
  elasticsearch {
    embedded => true
  }
}

